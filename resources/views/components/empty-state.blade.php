<div class="text-center py-4">
    <i class="bi bi-inbox fs-1 text-secondary"></i>
    <h4 class="text-secondary mt-3">No Items Found</h4>
    <p class="text-muted">{{ $message ?? 'There are no items to display at the moment.' }}</p>
</div>